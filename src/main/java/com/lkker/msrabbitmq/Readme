一、JPA模块：
1.使用原生SQL，实现复杂查询(多表联合查询)
    在Repository类中只能使用List<Map<String,Object>> 来接收查询得到的数据，然后通过自己转换。

2.使用JPQL实现多表联合查询
    使用jpa封装的jpql非常方便。在进行复杂查询时，①定义好返回结果集的对象类；
                                                 ②写查询语句；
                                                 ③分页和排序封装在Pageable中，只需要将pageable传入即可，jpa会自动帮我们处理。


fastjson :
JSONArray：相当于List<Object>
JSONObject：相当于Map<String, Object>
